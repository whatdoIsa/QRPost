# 기여 가이드

## 작업 흐름

1. 이슈를 먼저 등록한다. 모든 작업은 이슈에서 시작한다.
2. `develop`에서 `feature/#<이슈번호>-<영문슬러그>` 브랜치를 만든다. 예: `feature/#12-wire-format`
3. 작업 후 PR을 올리고 CI 통과를 확인한 뒤 스쿼시 머지한다. 병합한 브랜치는 삭제한다.

## 브랜치 전략

| 브랜치 | 역할 |
|---|---|
| `main` | 배포 브랜치. App Store 릴리즈 상태만 존재한다 |
| `develop` | 개발 기본 브랜치. 모든 작업 브랜치의 베이스 |
| `release` | 버전 안정화 후 main에 병합하고 `vX.Y.Z` 태그를 남긴다 |
| `feature/#N-슬러그` | 이슈 단위 작업 |
| `fix/#N-슬러그` | 버그 수정 |

## 커밋 규칙

- 제목: 작업 내용을 한국어로 간결하게 한 줄. 예: `분위수 출력과 예외처리, 폴백 강화`
- 스쿼시 머지 시 제목 끝에 이슈 번호가 붙는다: `... (#28)`
- 하나의 커밋은 하나의 논리적 변경만 담는다
- 본문(선택): 제목으로 부족할 때만, 무엇을·왜
- 금지: 이모지, AI 어시스턴트 서명, 근거 없는 수식어, 영어 장문

## PR 규칙

- 제목: 작업 요약, 커밋 제목과 같은 결
- 본문 첫 줄: `closes #이슈번호`
- 작업 내용 불릿 2~4개 — 수치와 사실 위주로 무엇이 바뀌었는지
- 마지막에 테스트 체크박스
- base는 `develop` (release → main 병합 제외)
- CI 통과 확인 후 스쿼시 머지, 브랜치 삭제

## 코드 스타일

- 주석, 식별자, 문자열, 커밋, PR 어디에도 이모지를 쓰지 않는다
- 과장 표현("간단히", "손쉽게", "강력한" 등)을 쓰지 않는다. 사실만 기술한다
- 주석은 코드가 표현할 수 없는 제약과 이유만 적는다
- 색·간격·모서리는 `DesignTokens.swift`의 토큰만 사용한다 (docs/디자인-가이드.md 참조)
