# 기여 가이드

## 브랜치 전략

| 브랜치 | 역할 |
|---|---|
| `main` | 배포 브랜치. App Store 릴리즈 상태만 존재한다 |
| `develop` | 개발 기본 브랜치. 모든 작업 브랜치의 베이스 |
| `release` | 버전 관리 브랜치. develop에서 안정화한 뒤 main에 병합하고 `vX.Y.Z` 태그를 남긴다 |
| `feature/<이름>` | 기능 단위 작업. develop에서 분기해 PR로 develop에 병합 |
| `fix/<이름>` | 버그 수정. 동일한 플로우 |

플로우: `feature/*` → `develop` → `release` → `main` (병합 시 버전 태그)

## 커밋 규칙

- 형식: `<type>: <제목>` — 제목은 한국어, 50자 이내, 명사형 종결
- type: `feat` `fix` `refactor` `test` `docs` `chore` `style` `perf` `build`
- 하나의 커밋은 하나의 논리적 변경(기능 단위)만 담는다
- 본문(선택): 무엇을, 왜 변경했는지. 어떻게는 코드가 설명한다
- 금지: 이모지, AI 어시스턴트 서명(Co-Authored-By 등), 근거 없는 수식어

## PR 규칙

- 제목은 커밋과 같은 형식 (`feat: ...`)
- base는 `develop` (release → main 병합 PR 제외)
- 본문은 템플릿(변경 사항 / 이유 / 테스트)을 채운다
- 병합 전 `cd QRPostCore && swift test` 통과를 확인한다
- 기능 브랜치는 병합 후 삭제한다

## 코드 스타일

- 주석, 식별자, 문자열, 커밋, PR 어디에도 이모지를 쓰지 않는다
- 과장 표현("간단히", "손쉽게", "강력한" 등)을 쓰지 않는다. 사실만 기술한다
- 주석은 코드가 표현할 수 없는 제약과 이유만 적는다
- 색·간격·모서리는 `DesignTokens.swift`의 토큰만 사용한다 (docs/디자인-가이드.md 참조)
